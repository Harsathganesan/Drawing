{
  "name": "drawing-backend",
  "version": "1.0.0",
  "description": "Backend for drawing order app with MongoDB Atlas",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test-connection": "node -e \"require('mongoose').connect(process.env.MONGODB_URI).then(()=>console.log('✅ Connection test passed')).catch(err=>console.error('❌ Connection test failed:',err.message))\""
  },
  "keywords": [
    "mongodb",
    "atlas",
    "express",
    "react",
    "drawing"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mongodb": "^7.1.0",
    "mongoose": "^8.0.0",
    "multer": "^2.1.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
